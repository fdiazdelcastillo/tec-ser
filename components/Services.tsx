import { CardComp } from "./Card";

type ServicesProps = {};

const Services: React.FC<ServicesProps> = (props) => {
  return (
    <section id="services">
      <CardComp />
    </section>
  );
};

export default Services;
